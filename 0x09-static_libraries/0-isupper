#include "main.h"

/**
 * _isupper - uppercase letters
 *
 *@c: it is 1 if c is uppercase else 0
 *Return: either 1 or 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
